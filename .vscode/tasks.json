{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Docker Image",
            "type": "shell",
            "command": "docker build -t my-sagemaker-image .",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "Run Docker Container",
            "type": "shell",
            "command": "docker run --rm -it -v ${workspaceFolder}/data:/opt/ml/input/data -v ${workspaceFolder}/output:/opt/ml/output --name sagemaker-train my-sagemaker-image",
            "problemMatcher": [],
            "dependsOn": "Build Docker Image"
        }
    ]
}
